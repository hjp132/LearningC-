@model OdeToFood.Web.Models.CuisineTypeViewModel

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm("Create", "CuisineType", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>CuisineType</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.LabelFor(model => model.Name, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "targetname form-control", data_max_length = "50", autocomplete = "off" } })
                @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            @*@Html.LabelFor( htmlAttributes: new { @class = "control-label col-md-2" })*@
            <label class="control-label col-md-2">Upload Image:</label>
            @* Ask Marc why TextBoxFor didn't work??' *@
            @*@Html.TextBoxFor(model => model.ImageFile, new { type = "file", name = "image" })*@
            <div class=" col-md-10">
                <input type="file" name="image" class="" required />
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

<script src="~/Scripts/jquery-3.6.0.min.js " type="text/javascript"></script>
<script src="~/Scripts/frontendScripts.js" type="text/javascript"></script>

